# Whisper Dictation Configuration
# Copy to ~/.config/whisper-dictation/config or ~/.whisper-dictation/config
# All paths support ~ expansion.

# Microphone: device index for avfoundation (0 = system default)
# Run: ffmpeg -f avfoundation -list_devices true -i "" to list devices
MIC_INDEX=0

# Whisper model path (ggml-small.bin recommended)
WHISPER_MODEL=~/whisper-models/ggml-small.bin

# Whisper CLI binary (auto-detected if not set)
# WHISPER_BIN=/opt/homebrew/bin/whisper-cli

# Sessions directory (audio, transcripts, PIDs, logs)
SESSIONS_DIR=~/whisper-sessions

# Trigger directory (Karabiner scripts write here; WhisperDictation app polls)
TRIGGER_DIR=~/.whisper-trigger

# Trigger file names (default: record-start, record-stop under TRIGGER_DIR)
# App and scripts use these paths. Override only if needed.
# RECORD_START_TRIGGER=~/.whisper-trigger/record-start
# RECORD_STOP_TRIGGER=~/.whisper-trigger/record-stop

# Silence detection: threshold (dB) and duration (seconds)
SILENCE_THRESHOLD_DB=-35
SILENCE_DURATION_SEC=8

# Prune sessions older than this many days
SESSION_RETENTION_DAYS=7
